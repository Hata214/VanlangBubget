'use client';

import HomePage from '@/app/page';

// Since this is a route segment for Next.js App Router,
// it needs to export a React component.
// We'll re-export the HomePage component for this locale-specific route.
export default HomePage;
